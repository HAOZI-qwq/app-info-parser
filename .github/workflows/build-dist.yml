name: Test and build browser dist

on:
  push:
    branches:
      - master
    paths:
      - 'lib/**'
      - 'bin/**'
      - 'test/**'
      - 'package.json'
      - '.babelrc'
      - '.github/workflows/build-dist.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  test-and-build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      - name: Install dependencies
        run: npm install --no-audit --no-fund

      - name: Run regression tests
        run: npm test

      - name: CLI smoke test
        run: node bin/index.js --version

      - name: Build browser distribution
        run: npm run dist

      - name: Verify generated bundles
        run: |
          test -s dist/app-info-parser-next.js
          test -s dist/app-info-parser-next.min.js
          if grep -q 'sourceMappingURL' dist/app-info-parser-next.min.js; then
            echo 'Unexpected stale sourceMappingURL in minified bundle'
            exit 1
          fi

      - name: Commit generated dist
        shell: bash
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add -A dist
          if git diff --cached --quiet; then
            echo 'dist is already up to date.'
          else
            git commit -m 'build: update browser dist / 更新浏览器构建产物'
            git pull --rebase origin master
            git push
          fi

  node-handle-windows:
    runs-on: windows-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      - name: Install dependencies
        run: npm install --no-audit --no-fund

      - name: Verify ZIP handles are released
        run: node test/node-unzip.test.js

      - name: Verify compressed app parsing
        run: node test/compressed-app.test.js
